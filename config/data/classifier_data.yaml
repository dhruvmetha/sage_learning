_target_: src.data.classifier_data.ClassifierDataModule
data_dir: ${data_dir}
batch_size: ${batch_size}
num_workers: ${num_workers}
image_size: ${image_size}
use_coord_grid: ${use_coord_grid}
use_local: ${use_local}
train_split: 0.9
