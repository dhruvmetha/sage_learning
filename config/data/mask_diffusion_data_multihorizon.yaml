# Multi-Horizon Mask Diffusion Data Config
# Returns target_goals [2, H, W] and solution_depth for masked loss

_target_: src.data.mask_diffusion_data_multihorizon.MultiHorizonDataModule
data_dir: ${data_dir}
batch_size: ${batch_size}
num_workers: ${num_workers}
pin_memory: true
image_size: ${image_size}
use_coord_grid: ${use_coord_grid}
train_split: 0.9
use_h5: ${use_h5}
