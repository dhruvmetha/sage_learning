_target_: src.model.dit.edge_crossattn.EdgeCrossAttn
img_size: ${image_size}
patch: 2          # 32x32 feature map (vs 16x16) -> finer per-edge gather
in_channels: ${context_channels}
dim: 192
scene_depth: 4
edge_depth: 4
heads: 6
num_depths: ${num_depths}
